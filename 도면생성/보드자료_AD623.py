# -*- coding: utf-8 -*-
"""AD623 판 — 1번·2번 보드의 결선과 부품값. **도면 스크립트가 함께 읽는 한 벌.**

<sub>2026-09-05 · AD623 모듈 2개가 도착해 TL072 3개 판에서 갈아탄 것</sub>

좌표 표기 `(열, 행)` 은 지난 TL072 판과 같다. **회로(전기적 연결)만 이전 설계에서 가져오고,
자리는 지난 판 배치 위에 새로 짰다** (2026-09-05 · 신이안 지시).

값의 출처
  [모듈]  AD623 은 DIP 칩이 아니라 이득 트리머가 실장된 모듈이다 — 핀 배치는 실물 확인
  [AD623] AD623 데이터시트 Rev. G — 바이어스 17 nA typ · 25 nA max (p.6 DUAL SUPPLIES)
  [부품값] 기록.md 「부품값을 정하는 식」
"""

# ── 두 판이 함께 쓰던 좌표계·색·잭 상수. 지난 판을 지우면서 여기로 옮겼다 ──
RAILGAP = 5                 # 레일은 5·11·17·23·29 열에 구멍이 없다
ROWS = list('ABCDEFGHIJ')
BAND = {                    # 저항 색띠 (5색띠 · 오차 ±1 % 금속피막)
    '10 kΩ':  ('갈', '검', '검', '빨', '갈'),
    '1 MΩ':   ('갈', '검', '검', '노', '갈'),
    '3.3 kΩ': ('주', '주', '검', '갈', '갈'),
    '220 Ω':  ('빨', '빨', '검', '검', '갈'),
    '1 Ω':    ('갈', '검', '검', '은', '갈'),
}
BANDCOL = {'검': '#1b1b1b', '갈': '#7a4a1e', '빨': '#c0392b', '주': '#e67e22',
           '노': '#e8c31a', '초': '#2c8c3c', '파': '#2166ac', '보': '#7b52ab',
           '회': '#9a9a9a', '흰': '#ffffff', '금': '#c9a227', '은': '#b8b8b8'}
CAPVAL = {'Cd': '104 · 0.1 µF', 'C1': '474 마일러 · 0.47 µF', 'Cf': '472 · 4.7 nF'}

# 선 색은 실물의 종류다 — 역할이 아니다
ALG = '#0f9b8e'             # 악어클립 테스트 리드선
SNP = '#8e44ad'             # 건전지 스냅 연선
JMP = '#1a5fb4'             # 수–수 점퍼선

JACK_ROW = 'J'
JACK_PINS = [(30, 'SLEEVE'), (31, 'RING2'), (32, 'RING1'), (33, 'TIP')]
JACK_WIRES = [None,                        # [0] 은 지난 판 전용이었다 — 아래에서 [1:] 만 쓴다
              ((30, 'G'), (28, 'BG'))]     # SLEEVE → 아래 GND 레일 (28열 · 레일이 끊기기 전)
JACK_POST = {'B1': ('⑤', '채널 1 오디오 케이블 → 인터페이스 입력 1'),
             'B2': ('⑥', '채널 2 오디오 케이블 → 인터페이스 입력 2')}

CD_COLS = (10, 16)          # 7열은 AD623 전원 점퍼가 쓴다 — TL072 판(7·16)과 다르다

# ══════════════════════════════════════════════════════════════════════
# 소자 배치
#   AD623 모듈  4–7열 · 윗핀 E행 / 아랫핀 I행
#       윗줄(A–E 쪽, 왼→오)  GND · OUT · REF · −VS
#       아랫줄(F–J 쪽, 왼→오) GND · IN+ · IN− · +VS
#       처음 도면의 E/F보다 핀 줄 간격이 아래로 3칸 더 길다 (2026-09-05 실물 확인).
#   TL072      19–22열 · 홈을 걸친다
#       위 반쪽(B)  1번 보드 = 가상접지 버퍼 · 2번 보드 = 남는 반쪽
#       아래 반쪽(A) 2단 (반전증폭 + 대역통과)
# ══════════════════════════════════════════════════════════════════════
_MOD = (4, 'AD623 모듈', '이득은 보드 위 트리머로', '핀 이름은 위아래에',
        ['GND', 'OUT', 'REF', '−VS'], ['GND', 'IN+', 'IN−', '+VS'], 'E', 'I')

B1_IC = [_MOD,
         (19, 'TL072', '아래 — 2단 (대역통과 + 이득)', '위 — 가상접지 버퍼')]
B2_IC = [_MOD,
         (19, 'TL072', '아래 — 2단 (대역통과 + 이득)', '위 — 남는 반쪽 · 묶어 둔다')]

# ══════════════════════════════════════════════════════════════════════
# 결선 — 수–수 점퍼선
# ══════════════════════════════════════════════════════════════════════
_전원 = [((18, 'BV'), (17, 'J')),      # V− 를 위쪽으로
        ((17, 'I'), (17, 'C')),
        ((9, 'C'), (22, 'C')),        # 분압 중점 → TL072 위 반쪽 +입력(5번 핀)
        ((20, 'B'), (20, 'TG'))]      # 가상접지 버퍼 출력 → 위 GND 레일 (1번 보드만)

_공통 = [((2, 'TG'), (2, 'BG')),       # 위 GND ↔ 아래 GND
        ((4, 'B'), (4, 'TG')),        # AD623 GND (윗줄)
        ((4, 'J'), (4, 'BG')),        # AD623 GND (아랫줄 · 모듈 핀은 I행)
        ((6, 'B'), (6, 'TG')),        # AD623 REF → GND  ★ 출력의 0 V 기준

        # 윗쪽 −VS를 모듈 왼쪽 3열에서 아래 전원 레일로 내린다.
        # 실물 모듈이 3열 I행을 가리므로 B행에서 V− 레일로 한 번에 내린다.
        ((7, 'C'), (3, 'C')),
        ((3, 'B'), (3, 'BV')),

        # 아래쪽 +VS를 모듈 오른쪽 8열에서 윗 전원 레일로 올린다.
        ((7, 'J'), (8, 'J')),
        ((8, 'I'), (8, 'C')),
        ((8, 'B'), (8, 'TV')),

        # 윗쪽 OUT을 10열에서 아래 필터 입력으로 내린다.
        ((5, 'B'), (10, 'B')),
        ((10, 'C'), (10, 'J')),

        # 아래쪽 IN+를 9열로 빼서 전극과 Rb1이 서로 다른 구멍을 쓰게 한다.
        ((5, 'J'), (9, 'J')),
        ((12, 'J'), (13, 'BG')),      # Rb1의 GND쪽 노드

        ((19, 'B'), (19, 'TV')),      # TL072 8번
        ((22, 'I'), (22, 'BV')),      # TL072 4번
        ((21, 'G'), (21, 'BG')),      # 2단 +입력(3번 핀) → GND
        ((19, 'J'), (24, 'J')),       # 2단 출력(1번 핀) 연장
        ((24, 'F'), (34, 'F'))] + JACK_WIRES[1:]   # 출력 → 잭 · SLEEVE → GND

B1_WIRES = _전원 + _공통
B2_WIRES = _공통


# ══════════════════════════════════════════════════════════════════════
# 부품
# ══════════════════════════════════════════════════════════════════════
def P_(p1, p2, name, val, role='', bw=38, lp=None, anc='middle', cap=False, short=None):
    return dict(p1=p1, p2=p2, name=name, val=val, role=role,
                bw=bw, lp=lp, anc=anc, cap=cap, short=short or val)


# 전원부 — 1번 보드에만
PWR = [
    P_((9, 'TV'), (9, 'A'), 'R1', '10 kΩ', '9 V 를 반으로 나눈다',
       lp=lambda cx, Y: (cx(9) + 24, (Y('TV') + Y('A')) / 2), anc='start'),
    P_((17, 'B'), (9, 'B'), 'R2', '10 kΩ', '9 V 를 반으로 나눈다',
       lp=lambda cx, Y: (cx(13), Y('B') + 26)),
    P_((20, 'D'), (21, 'D'), '1 Ω', '1 Ω', '가상접지 버퍼 되먹임', bw=30,
       lp=lambda cx, Y: (cx(19.4), Y('D')), anc='end'),
]

# 두 보드에 똑같이
COMMON = [
    # ★ Rb1 과 Rb2 는 **반드시 같은 값** — AD623 바이어스 17 nA 가 1 MΩ 을 지나면 17 mV 다.
    #   한쪽만 달면 그 17 mV 가 통째로 차동 오프셋이 된다. 양쪽을 맞추면 남는 것은
    #   오프셋 전류뿐이라 0.25 nA × 1 MΩ = 0.25 mV 로 68배 줄어든다.
    P_((9, 'G'), (12, 'G'), 'Rb1', '1 MΩ', '★ Rb2 와 같은 값이어야 한다', bw=30,
       lp=lambda cx, Y: (cx(10.5), Y('G') - 24)),
    P_((6, 'J'), (6, 'BG'), 'Rb2', '1 MΩ', '★ Rb1 과 같은 값이어야 한다',
       lp=lambda cx, Y: (cx(6.7), (Y('J') + Y('BG')) / 2 + 6), anc='start'),
    P_((10, 'I'), (13, 'I'), 'C1', CAPVAL['C1'], '직류를 끊는다', bw=32, cap=True, short='474',
       lp=lambda cx, Y: (cx(11.5), Y('I') - 24)),
    P_((13, 'H'), (20, 'H'), 'Rin', '3.3 kΩ', '고역차단 103 Hz',
       lp=lambda cx, Y: (cx(16.5), Y('H') - 24)),
    P_((24, 'I'), (20, 'I'), 'Rf', '10 kΩ', '2단 이득 3.03',
       lp=lambda cx, Y: (cx(22), Y('I') + 30)),
    P_((24, 'G'), (20, 'G'), 'Cf', CAPVAL['Cf'], '저역차단 3.39 kHz', bw=32, cap=True, short='472',
       lp=lambda cx, Y: (cx(22), Y('G') - 24)),
    # 출력 — 잭 브레이크아웃 (암–수 점퍼선으로 보드 옆에서 끌어온다)
    P_((34, 'G'), (33, 'G'), '10 µF', '전해 10 µF · 50 V', '★ + 를 TIP(33열) 쪽으로', bw=26,
       cap=True, short='전해 50 V', lp=lambda cx, Y: (cx(34.8), Y('G')), anc='start'),
    P_((31, 'H'), (30, 'H'), '1 Ω', '1 Ω', 'RING2 ↔ SLEEVE', bw=26,
       lp=lambda cx, Y: (cx(29.4), Y('H')), anc='end'),
    P_((32, 'I'), (31, 'I'), '1 Ω', '1 Ω', 'RING1 ↔ RING2', bw=26,
       lp=lambda cx, Y: (cx(32.7), Y('I')), anc='start'),
]

# 2번 보드에만 — TL072 위 반쪽이 남으므로 묶어 둔다
B2_ONLY = [
    P_((20, 'D'), (21, 'D'), '1 Ω', '1 Ω', '남는 반쪽 되먹임', bw=30,
       lp=lambda cx, Y: (cx(19.4), Y('D')), anc='end'),
    P_((22, 'A'), (22, 'TG'), '10 kΩ', '10 kΩ', '남는 반쪽 +입력을 GND 로',
       lp=lambda cx, Y: (cx(22.7), (Y('A') + Y('TG')) / 2), anc='start'),
]

B1_PARTS = PWR + COMMON
B2_PARTS = B2_ONLY + COMMON

# ══════════════════════════════════════════════════════════════════════
# 보드 밖으로 나가는 자리 — 번호는 TL072 판과 같다
# ══════════════════════════════════════════════════════════════════════
B1_POSTS = [((9, 'H'), '①', ALG, '기록 전극 1 (AD623 IN+)'),
            ((12, 'BG'), '③', ALG, '기준(접지) 전극'),
            ((15, 'TG'), '⑨', ALG, '호일 케이지 (선택)'),
            ((1, 'TV'), '⑦', SNP, '건전지 빨강'), ((1, 'BV'), '⑧', SNP, '건전지 검정'),
            ((0, 'TV'), '④', JMP, '레일 V+ → 2번 보드'), ((0, 'BG'), '④', JMP, '레일 GND → 2번 보드'),
            ((0, 'BV'), '④', JMP, '레일 V− → 2번 보드')]
B2_POSTS = [((9, 'H'), '②', ALG, '기록 전극 2 (AD623 IN+)'),
            ((0, 'TV'), '④', JMP, '레일 V+ ← 1번 보드'), ((0, 'TG'), '④', JMP, '레일 GND ← 1번 보드'),
            ((0, 'BV'), '④', JMP, '레일 V− ← 1번 보드')]


# ══════════════════════════════════════════════════════════════════════
# 도면 머리글
# ══════════════════════════════════════════════════════════════════════
TITLE = {'B1': '브레드보드 배치도 — 1번 보드 · 채널 1   [AD623 판]   전원부가 여기 있다',
         'B2': '브레드보드 배치도 — 2번 보드 · 채널 2   [AD623 판]   전원부가 없다 (레일 3줄을 1번에서)'}
_N = [
 ('★ 저항마다 이름 아래 붉은 글씨가 실제로 꽂는 값이다. 몸통의 색띠도 그 값의 띠다. 그래도 꽂기 전에 멀티미터로 잰다.', 1),
 ('★ Rb1 과 Rb2 는 반드시 같은 1 MΩ 이다 — AD623 바이어스 전류 17 nA 가 1 MΩ 을 지나면 17 mV 다. 한쪽만 달면 그것이 통째로 직류 오프셋이 되어 증폭된다.', 1),
 ('★ 이득 저항 Rg 를 꽂지 않는다 — 모듈 위의 트리머로 맞춘다. 이득을 100 이하로 둔다 (그 위로 가면 대역폭이 3 kHz 아래로 떨어진다).', 1),
 ('★ 차동단 저항 네 개가 없어졌다 — CMRR 이 칩 안에서 공장 트리밍돼 있다. 10 kΩ 40개를 재서 고르는 일이 필요 없다.', 1),
 ('★ 잭 브레이크아웃은 보드에 직접 못 꽂는다 (레일에만 걸린다) — 암–수 점퍼선 4개로 30–33열 J행까지 뺀다. TIP 이 33열이다.', 1),
 ('★ 레일 구멍은 5개마다 한 칸씩 비어 있다 — 5 · 11 · 17 · 23 · 29 열에 구멍이 없다. 레일은 30열 부근에서 끊긴다.', 1),
 ('★ AD623 실물 방향: 윗 E행은 GND·OUT·REF·−VS, 아래 I행은 GND·IN+·IN−·+VS다.', 1),
 ('★ 모듈이 3열 I행을 가리므로 −VS는 3열 B행에서 아래 V− 레일 3열로 점퍼 하나만 꽂는다.', 1),
 ('★ 아랫 IN+는 9열로 빼서 전극과 Rb1을 나눠 꽂고, 윗 OUT은 10열을 거쳐 아래 필터로 내린다.', 1),
 ('AD623 모듈과 TL072 는 왼쪽이 GND(또는 1번) 쪽이 되게 꽂는다.  총 이득 = 트리머(100 이하) × 3.03.', 0),
 ('보드는 열 0–60 · 행 A–E / F–J 다. 여기는 0–36 만 그렸다. 선 색 = 실물의 종류 (파랑 점퍼선 · 청록 악어클립 · 보라 건전지 스냅 · 검정 오디오).', 0)]
NOTES = {'B1': _N + [('1번 보드에만 전원부(R1 · R2 · 가상접지 버퍼)가 있다. 2번 보드는 레일 3줄로 여기서 전원을 받는다.', 1)],
         'B2': _N + [('2번 보드의 남는 TL072 반쪽은 출력과 −입력만 잇는다. 출력을 GND 레일에 직결하면 1번 보드의 가상접지 버퍼와 충돌하므로 금지한다.', 1)]}
