# CLAUDE.md — 이 저장소에서 일하는 AI를 위한 지침

이 저장소는 STSY 연구의 실무 저장소다. 담당은 신이안(측정장치팀).

---

## 시작

세션을 시작하면 **`현재.md` → `로드맵.md` 두 개만** 읽는다.
특정 주제가 화제가 되면 그때 해당 노트를 연다.

**세션 시작에 반드시 읽는 문서를 늘리지 마라.**

---

## 태도

- **비판적 검증이 기본값이다.** 약점·반례·더 나은 대안을 먼저 찾는다.
- 막연한 칭찬 금지. 솔직하고 직설적으로, 핵심부터 간결하게.
- **방향은 신이안이 정한다.** 큰 결정과 아이디어는 그의 머리에서 나온다.
  너의 몫은 검토·조사·계산·코딩이다.
- 신이안은 네가 틀린 것을 잡아내 주기를 기대한다. 실제로 그가 너를 잡아낸 적도 있다
  (→ [`결정기록.md`](결정기록.md) 「철회된 주장」).

---

## 인용 규칙 ★ 가장 중요

- **인용하기 전에 `문헌/원문/`의 PDF를 열어 확인한다.**
  노트에 적힌 것을 사실로 믿지 마라. 노트는 요약이고, 요약은 왜곡될 수 있다.
  PDF는 `pymupdf`로 텍스트를 뽑아 읽는다
  (`pip install pymupdf`. PDF 렌더링 도구가 없어도 텍스트 추출은 된다).
- **문헌이 말한 것과 우리가 추론한 것을 반드시 구분해 표기한다.**
- **숫자에는 출처를 붙인다.** 논문명 + 어느 절/표에서 나온 값인지.
- **가설은 가설이라고 쓴다.** 검증 안 된 것을 사실처럼 쓰지 마라.
- **기전을 다른 종으로 옮길 때는 이전 가능성을 따로 논증한다.**
  (`결정기록.md`의 수초 철회 사건이 이 규칙의 근거다)
- 없는 문장을 인용하지 마라. 없는 결과를 지어내지 마라. **결론이 없으면 없는 것이다.**
### 원문을 못 구했을 때 ★ 링크를 주지 마라, 편지를 써라

**신이안에게 URL을 주고 받아오라고 하지 마라. 그 방식은 실패했다.**
(PMC 프루프오브워크·유료 장벽 때문에 브라우저로도 안 되는 경우가 많다.)

**대신 교신저자에게 보낼 메일 초안을 완성된 형태로 써서 준다.**
신이안이 그대로 복사해 보낸다.

**메일 초안에 반드시 들어갈 것:**
1. **수신 주소** — **반드시 원문 PDF나 논문 페이지에서 확인한 실제 주소.**
   **절대 형식을 추측해 만들지 마라**(`이름.성@대학.ac.kr` 같은 조합 금지).
   주소를 못 찾으면 **"주소 미확인"이라고 쓰고 그렇게 보고한다.**
2. **제목** — 논문 서지가 들어간 한 줄
3. **본문** — 영문. 신분(한국 고등학생·국제 연구 프로그램) · 무엇을 왜 필요로 하는지 ·
   **구체적으로 어느 그림/부록/수치가 필요한지** · 감사 인사
4. 서명 — Ian Shin / Bugil Academy, South Korea

**보내기 전에 확인할 것:**
- **저자가 생존해 있는가.** 오래된 논문은 반드시 확인한다.
  (Drewes 1999의 `cdrewes@iastate.edu`는 고인의 주소였다 — ISU 추모 장학금으로 확인)
- 공저자 중 이미 연락한 사람이 있으면 **중복해서 보내지 말고 그 편에 얹는다.**

**구매를 제안하기 전에 무료 경로를 전부 확인한다.**
순서: ① **Unpaywall** (`https://api.unpaywall.org/v2/{DOI}?email=...`) — DOI만 있으면
저자 기탁본·기관 리포지토리를 전부 훑는다 ② PMC · Europe PMC
(`https://www.ebi.ac.uk/europepmc/webservices/rest/{PMCID}/fullTextXML`) ③ HAL · 저자 홈페이지
④ **교신저자 메일 요청** ⑤ 그래도 없으면 구매 링크 제시.
**Mazzia 2018이 "유료"로 분류돼 있다가 HAL 무료본이 발견된 사건이 이 규칙의 근거다.**

**메일 초안은 [`브리지.md`](브리지.md)가 아니라 [`현재.md`](현재.md)에 둔다.**
보내고 나면 지우고, 회신 결과만 남긴다.

### 원문 미확보 표기 규칙

지금 이 저장소의 문헌 노트는 **대부분 원문 미확보 상태**다.
원문을 열어 확인하지 않은 인용에는 반드시 `· 원문 미확보`를 붙인다.

```
(Günther 1976 §3.2 · 원문 미확보)
```

PDF를 확보해 직접 확인한 뒤에는 그 표기를 지우고, 확인한 절·표 번호로 바꾼다.
확인 결과 값이 달랐으면 노트를 고치고 `결정기록.md`에 정정 사실을 남긴다.

---

## 기록 작성

- **사실만.** 무엇을 했나 · 뭐가 결정됐나 · 외부 정보.
- **추천을 기록 노트에 쓰지 마라.** 다음 AI가 그걸 정답으로 읽어 선택을 편향시킨다.
  판단과 열린 것은 [`현재.md`](현재.md) 한 곳에만.
- **복사하지 말고 링크해라.** 파생 데이터를 저장하지 마라.
- 구식은 지우고 현재본으로 교체한다. 덧쓰지 마라.
- 문서 본문에 회고를 섞지 마라("옛날엔 A였는데", "왜 바꿨냐면").
  **기각된 대안과 그 이유는 [`결정기록.md`](결정기록.md)에만** 쓴다 — 재작업을 막기 위해 별도 격리한다.
- **사실 정정은 허용, 해석 추가는 금지.**

---

## 데이터

- **원시 데이터는 성역이다.** 고치지도 지우지도 않는다.
- 실험 폴더에 `메타.md` 없이 데이터만 있으면 **그 데이터는 무효다.**
  온도 · 전극간격 · 자극방식 · 마취여부 · 장비설정이 반드시 함께 기록되어야 한다.
- 파일명에 날짜 · 단계 · 개체ID · 조건이 들어간다.

---

## 볼트와의 경계

- 신이안은 별도의 저장소 **Vault**를 갖고 있다(생기부 · 진로 관리).
  **너는 볼트를 보지 못하고, 볼트 담당 AI도 여기를 보지 못한다.**
  이 분리는 사고가 아니라 설계다 — 생기부 판단이 연구 저장소로 새지 않게 한다.
- **생기부 · 세특 · 진로 판단은 여기서 하지 마라.** 그건 볼트 소관이다.
- 볼트에 반영이 필요한 일이 생기면 [`브리지.md`](브리지.md)의 「→ 볼트로 보낼 것」에
  **볼트에 그대로 붙여넣을 수 있는 완성된 문장으로** 적는다. 대상 파일 경로를 명시한다.
- 볼트 작성 원칙(그쪽에 쓸 문장은 이 규칙을 따라야 한다):
  사실만 · 추천 금지 · 회고 금지 · 복사하지 말고 링크 · 제출 원문과 생기부 문구는 성역.
- **양쪽을 동시에 보는 것은 신이안이 명시적으로 요청할 때만.**
  방법과 한계는 [`브리지.md`](브리지.md) 「AI가 양쪽을 보게 하려면」.
- ⚠ **이 절과 [`브리지.md`](브리지.md)는 볼트의 존재를 드러낸다.**
  저장소를 공유하거나 공개할 계획이 생기면 **먼저 이 둘을 지우고 git 이력도 재작성한다.**

---

## 링크 · 커밋

### 링크는 표준 마크다운으로 쓴다

```
[로드맵](로드맵.md)          ✅
[[로드맵]]                    ❌ GitHub에서 글자 그대로 나온다
```

**옵시디언에서도 마크다운 링크가 그대로 동작하고, 파일 이름을 바꾸면 자동으로 따라 고쳐준다.**
설정 → 파일 및 링크 → **"내부 링크 자동 업데이트" 켜기**,
**"위키링크 사용" 끄기**. 이러면 옵시디언의 이름 변경 안전성과 GitHub 호환성을 둘 다 얻는다.

### 커밋 전 검사

```bash
python3 분석/src/check_links.py
```

깨진 링크 · 위키링크 · 망가진 표를 잡는다.
`git config core.hooksPath .githooks` 를 한 번 실행해 두면 커밋할 때 자동으로 돈다.
**클론을 새로 하면 다시 실행해야 한다.**

### 커밋

- **자동 커밋은 없다.** 작업이 끝나면 커밋하고 **푸시까지 한 뒤 답변에 해시를 적는다.**
- 이름을 바꾸면 그 파일을 가리키는 링크를 **같은 커밋에서** 고친다 (위 검사기가 확인해 준다).
- **여러 파일을 한꺼번에 문자열 치환할 때는 치환 후 반드시 검사기를 돌린다.**
  표가 반쯤 남는 사고가 실제로 있었다.
- **AI 도구의 자체 메모리에 저장하지 마라. 이 저장소가 유일한 기억 장치다.**

---

## 범위

- **우리 몫**: 측정장치 · 신호처리 · 문헌.
- **다른 팀원 몫**: 중금속 농도 · 노출 방식 · 완화물질 선별. **요청 없이 파고들지 마라.**
- 예산 감각: 30만원 이상 가능하나 **비싼 건 피한다. 돈을 날리면 안 된다.**
  구매를 제안할 때는 **국내 재고와 배송일을 반드시 확인**하고, 기각한 대안도 함께 적는다.
- GitHub · Reddit · 오픈소스 자료를 적극적으로 찾아 활용한다.

### 이 저장소가 하지 않는 것

볼트 관리 · 생기부/세특 조언 · 진로 상담 · 학습 계획.
그건 신이안의 다른 저장소 Vault 담당 AI의 소관이다. 여기서 하지 마라.
