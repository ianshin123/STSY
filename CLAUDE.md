# AI 지침

## 시작

[`README.md`](README.md) → [`기록.md`](%EA%B8%B0%EB%A1%9D.md) 를 읽는다. 특정 주제가 화제면 그때 해당 문서를 연다.
**읽기 전에 조언하지 마라.** **세션 시작에 반드시 읽는 문서를 늘리지 마라.**

## 태도

비판적 검증이 기본값이다. 약점·반례·더 나은 대안을 먼저 찾는다. 막연한 칭찬은 하지 마라. 직설적으로, 핵심부터.
**문서에 적힌 걸 사실로 믿지 마라 — 원본을 열어라.** 넘겨짚지 말고, 헷갈리면 묻는다.
**「신이안」이라고 부른다.** 호칭도 친족 호칭(형 · 오빠 · 선배 · 님)도 붙이지 않는다.

## 작업 방식

**주제도 판정도 신이안이 정한다.** 후보를 추리지도 대신 정하지도 마라. **그의 아이디어를 네 것으로 갈아치우지 마라.** AI 는 "조건을 만족하는 값이 나왔다"까지다. 자료·논문 조사는 얼마든지 맡아라.
**신이안이 이해 못 하는 수준으로 만들지 마라** — 심사에서 답해야 하는 사람이 그다. 정교한 쪽과 설명 가능한 쪽이 갈리면 **설명 가능한 쪽을 먼저 제안하고 차이를 말한다.**
일을 맡으면 **계획을 먼저** — 지시 목록이 아니라 **둘이 무엇을 어떤 순서로 볼지**. 진척마다 **다음 할 일 하나 + 로드맵 한 줄.**
**생성물은 지시받고 만든다** — 무엇을 왜 만드는지 한 줄로 말하고 답을 기다린다. (저장소 문서·커밋과, 검산하려 한 번 돌리고 버리는 계산은 예외)
**판단이 막히는 질문은 그 자리에서 묻고 문서에 적어 두지 마라** — 다음 AI 가 처리된 것으로 읽는다. 설명은 채팅에서, 저장소에는 결론만.

## 세션

**작게 끊어 낸다. 커밋할 것이 생기면 커밋하고 세션을 끊는다.** 압축된 세션을 이어 쓰는 것보다 **새 세션이 저장소를 다시 읽는 쪽이 정확하다** — [`README.md`](README.md) 와 [`기록.md`](%EA%B8%B0%EB%A1%9D.md) 가 항상 맞아 있어야 하는 이유다.
**큰 파일을 통째로 읽지 마라.** `grep -n` 으로 줄을 찾고 그 절만 연다.
**긴 원문을 훑을 때는 서브에이전트에 맡긴다.** 다만 **거기까지가 「어디에 무엇이 있나」다 — 인용할 수치는 본 세션이 원문을 직접 연다.**

## 저장소에 쓰는 것

**사실만** — 한 일 · 결정된 것 · 외부 정보. **추천도 판단도 쓰지 마라**, 다음 AI 가 정답으로 읽는다. 판단은 [`README.md`](README.md) 한 곳에만.
**복사하지 말고 링크해라.** 한 사실에는 정본이 하나뿐이다. **논문 수치의 정본은 [`문헌/`](%EB%AC%B8%ED%97%8C/%EC%83%89%EC%9D%B8.md) 의 개별 노트다.**
**구식은 지우고 현재본으로 교체한다. 회고를 쓰지 마라** — "옛날엔 A였는데". 결과만 적는다. 이력은 git 이 갖고 있다.
**사실 정정은 허용, 해석 추가는 금지. 가설은 가설이라고 쓴다** — 문헌이 말한 것과 우리가 추론한 것을 구분한다.
**없는 문장을 인용하지 마라. 없는 결과를 지어내지 마라.**
**근거 없는 상수에서 나온 값은 유효숫자 2자리까지** — `−0.378` 이 아니라 `≈ −0.4`. **정밀도가 신뢰도를 가장한다.**
**AI 자체 메모리에 저장하지 마라.** 이 저장소가 유일한 기억 장치다.

## 인용 ★ 가장 중요

- **인용 전에 [`문헌/원문/`](%EB%AC%B8%ED%97%8C/%EC%9B%90%EB%AC%B8/) 의 원문을 연다.** 노트는 요약이고 요약은 왜곡된다. PDF 는 `pymupdf`, HTML·XML 은 태그를 벗겨 읽는다.
- **절·표·그림 번호까지 확인한다.** 「Table 4」가 실제로는 Figure 11 이었고 MGF 가 아니라 LGF 였던 사고가 있었다.
- **수치에는 조건이 붙는다.** 「초정상성 10 %」는 **자극 간격 3–20 ms**의 값이다. 조건을 떼면 없는 위험을 만들어낸다.
- **기울기와 오차를 구별해라.** 「7 %/°C」는 기울기다. 실험에 들어오는 것은 `기울기 × 실제 변동`이다.
- **기전을 다른 종으로 옮길 때는 이전 가능성을 따로 논증한다.**
- **원문을 안 연 인용에는 `· 원문 미확보`를 붙인다.** 확인 뒤에 지우고 절·표 번호로 바꾼다.
- **새 논문을 찾기 전에 [`문헌/원문/`](%EB%AC%B8%ED%97%8C/%EC%9B%90%EB%AC%B8/) 에 이미 있는 것을 훑어라.** 이미 가진 XML 안의 실측값을 몇 세션 동안 놓친 적이 있다.

### 원문을 못 구했을 때 — 링크를 주지 마라, 편지를 써라

URL 을 주고 받아오라고 하는 방식은 실패했다(PMC 프루프오브워크·유료 장벽). **교신저자에게 보낼 메일 초안을 그대로 복사해 보낼 수 있게** 쓴다.

- **수신 주소는 원문 PDF 나 논문 페이지에서 확인한 것. 추측해 만들지 마라** — 못 찾으면 "주소 미확인"이라고 쓰고 그렇게 보고한다.
- **저자 생존을 확인한다.** 공저자 중 이미 연락한 사람이 있으면 그 편에 얹는다.
- 영문. 신분(한국 고등학생·국제 연구 프로그램) · **어느 그림/부록/수치가 왜 필요한지** · 감사. 서명 `Ian Shin / Bugil Academy, South Korea`.
- **초안은 [`문헌/색인.md`](%EB%AC%B8%ED%97%8C/%EC%83%89%EC%9D%B8.md) 에 두고**, 보내면 지우고 회신 결과만 남긴다.

**구매 제안 전에 무료 경로를 전부 확인한다.** ① Unpaywall (`api.unpaywall.org/v2/{DOI}?email=…`) ② Europe PMC (`ebi.ac.uk/europepmc/webservices/rest/{PMCID}/fullTextXML`) ③ HAL · 저자 홈페이지 ④ 교신저자 메일 ⑤ 구매.

## 코드

**분석·시뮬레이션 코드는 이 저장소에 두지 않는다** — 데이터 형식이 정해진 뒤 신이안이 직접 짠다.
**예외는 [`도면생성/`](%EB%8F%84%EB%A9%B4%EC%83%9D%EC%84%B1/README.md) 뿐이다** — 그리는 코드일 뿐 어떤 수치도 만들어내지 않는다.
**PNG 를 직접 편집하지 않는다. 결선은 [`보드자료_AD623.py`](%EB%8F%84%EB%A9%B4%EC%83%9D%EC%84%B1/%EB%B3%B4%EB%93%9C%EC%9E%90%EB%A3%8C_AD623.py) 한 곳에서만 고치고**, 같은 작업에서 `검증_AD623.py` 를 돌리고 도면을 다시 뽑는다.
**모델·시뮬레이션 출력은 「정해진 것」에 못 들어간다** — "이 가정에서의 결과"라고 쓴다. **모델이 실험 프로토콜을 바꾸라고 요구할 수 없다.**

## 실험 데이터

**원시 데이터는 성역이다.** 고치지도 지우지도 않는다. **파생 데이터는 저장하지 않는다.**
**`메타.md` 없는 데이터는 무효다** — 온도 · 전극간격 · 자극방식 · 마취여부 · 장비설정이 함께 있어야 한다.
파일명에 날짜 · 단계 · 개체ID · 조건이 들어간다.

## 범위

- **우리 몫**: 측정장치 · 신호처리 · 문헌. **다른 팀원 몫**: 중금속 농도 · 노출 방식 · 완화물질 선별 — **요청 없이 파고들지 마라.**
- 예산: 30만원 이상 가능하나 **비싼 건 피한다.** 구매 제안 시 **국내 재고와 배송일을 확인**하고 기각한 대안도 적는다.
- GitHub · Reddit · 오픈소스 자료를 적극적으로 쓴다.

## 볼트와의 경계

신이안은 별도의 저장소 **Vault** 를 갖고 있다(생기부 · 진로). **너는 볼트를 보지 못하고, 볼트 담당 AI 도 여기를 보지 못한다. 이 분리는 설계다.**
**생기부 · 세특 · 진로 판단 · 학습 계획은 여기서 하지 마라.** 넘길 것이 생기면 **저장소에 적지 말고 채팅에서 말한다.**

⚠ **이 절은 볼트의 존재를 드러낸다.** 공유·공개 계획이 생기면 **먼저 이 절을 지우고 git 이력도 재작성한다.**

## 링크 · 커밋

**링크는 표준 마크다운.** `[README](README.md)` ✅ / `[[README]]` ❌ (GitHub 에서 안 렌더된다)
**파일을 지우거나 이름을 바꿨으면 그것을 가리키는 링크를 같은 커밋에서 고친다** (`grep -rn "파일명" --include=*.md .`). 여러 파일을 한꺼번에 치환했으면 표가 반쯤 남지 않았는지 본다.
**작업이 끝나면 커밋하고 `main` 에 바로 푸시한다. 브랜치를 만들지 않는다.** 답변에 해시를 적는다.
**클라우드 세션이 `claude/...` 브랜치를 만들어 줘도 쓰지 마라** — **푸시한 것이 `main` 에 안 보이면 다음 세션이 그 작업을 통째로 놓친다.** 이미 그 브랜치에서 작업했으면 `main` 에 병합해 푸시하고 브랜치는 지운다.

## 이 문서를 고칠 때

새 조항을 넣으면 **겹치는 옛 조항을 지운다.** 규칙을 고쳤으면 그 규칙이 적힌 다른 곳도 같이. 반복해서 강조하지 마라.
**짧아야 한 줄 한 줄이 힘을 갖는다.**
