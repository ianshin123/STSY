# 브리지 — Vault ↔ STSY 우편함

신이안은 별도의 저장소 **Vault**를 갖고 있다 (생기부 · 진로 관리).
**이 저장소의 AI는 볼트를 보지 못하고, 볼트 담당 AI도 여기를 보지 못한다.**

그래서 이 파일이 두 저장소 사이의 통로다.
**옮기는 일은 신이안이 직접 한다.**

---

## 규칙

### 「→ 볼트로 보낼 것」에 쓸 때

- **볼트에 그대로 붙여넣을 수 있는 완성된 문장으로** 쓴다
- **대상 파일 경로를 명시한다**
- 볼트 작성 원칙을 지킨 문장이어야 한다:
  **사실만 · 추천 금지 · 회고 금지 · 복사하지 말고 링크 ·
  제출 원문과 생기부 문구는 성역**
- 옮긴 항목은 **「보낸 것」으로 내리고 날짜를 적는다**

### 여기서 하지 않는 것

**생기부 · 세특 · 진로 판단은 이 저장소에서 하지 마라.** 그건 볼트 소관이다.
여기서는 "볼트에 이런 사실이 생겼다"만 전한다.

### 이 저장소가 공개될 수 있는 상태면 이 파일을 지운다

이 파일과 [`CLAUDE.md`](CLAUDE.md)의 「볼트와의 경계」 절은
**볼트의 존재와 목적을 드러낸다.**
공동작업자를 들이거나 저장소를 공개로 돌릴 계획이 생기면 **먼저 이 둘을 지운다.**
파일만 지우면 git 이력에 남으므로 이력 재작성도 함께 해야 한다.

---

## 사람이 두 저장소를 오갈 때 — 옵시디언 URI

옵시디언은 **다른 보관함의 노트로 바로 뛰는 링크**를 지원한다.
STSY 노트 안에서 볼트 노트로 한 번에 갈 수 있다.

```
[볼트의 STSY 노트](obsidian://open?vault=Vault&file=STSY)
```

- `vault=` 에 **보관함 이름**, `file=` 에 **확장자 뺀 경로**를 넣는다
- 이름에 공백·한글이 있으면 URL 인코딩한다 (공백은 `%20`)
- **옵시디언 앱에서만 동작한다.** GitHub 웹에서는 죽은 링크다 —
  이 저장소는 비공개이고 주로 옵시디언에서 읽으므로 감수할 만하다
- [`분석/src/check_links.py`](분석/src/check_links.py)가 `obsidian://` 를 외부 링크로
  취급하도록 해뒀다. 깨진 링크로 잡히지 않는다

**반대 방향(볼트 → STSY)도 같은 방식으로 볼트 쪽에 걸면 된다.**

**단, 이건 사람이 오가는 통로일 뿐 AI가 양쪽을 보는 것과는 다르다.**
AI에게 양쪽을 보여주는 방법은 아래 「AI가 양쪽을 보게 하려면」.

---

## AI가 양쪽을 보게 하려면

**기본값은 "보지 못한다"이고, 그게 의도된 설계다.**
분리가 있어야 생기부 판단이 연구 저장소로 새지 않는다.

정말 필요할 때만, **그 세션에 한해** 볼트를 붙일 수 있다:

- Claude Code 세션에서 `add_repo` 로 볼트 저장소를 붙인다 (승인 필요)
- 그 세션 동안만 유효하다. 다음 세션은 다시 분리 상태로 시작한다
- **신이안이 명시적으로 요청할 때만 한다.** AI가 알아서 붙이지 않는다

**git submodule 로 영구히 잇는 방법은 쓰지 않는다** —
볼트 내용이 STSY 작업 트리로 들어오고, 저장소를 공유할 일이 생기면
서브모듈 포인터가 볼트의 존재와 주소를 드러낸다.

---

## → 볼트로 보낼 것

### 1. STSY 저장소 링크 추가

**대상 파일**: 볼트의 `STSY.md`

**추가할 문장**:

> 연구 실무(장치 설계 · 신호 처리 · 문헌)는 STSY 저장소에 있다 —
> https://github.com/ianshin123/STSY

---

## ← 볼트에서 온 것

*(아직 없음)*

---

## 보낸 것

*(아직 없음)*
