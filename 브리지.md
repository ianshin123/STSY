# 브리지 — Vault ↔ STSY 우편함

신이안은 별도의 저장소 **Vault**를 갖고 있다 (생기부 · 진로 관리).
**이 저장소의 AI는 볼트를 보지 못하고, 볼트 담당 AI도 여기를 보지 못한다.**
이 분리는 사고가 아니라 설계다.

**옮기는 일은 신이안이 직접 한다.**

---

## 규칙

- 「→ 볼트로 보낼 것」에는 **볼트에 그대로 붙여넣을 수 있는 완성된 문장**과
  **대상 파일 경로**를 함께 적는다
- 볼트 작성 원칙을 지킨 문장이어야 한다:
  **사실만 · 추천 금지 · 회고 금지 · 복사하지 말고 링크 · 제출 원문과 생기부 문구는 성역**
- 옮긴 항목은 **「보낸 것」으로 내리고 날짜를 적는다**
- **생기부 · 세특 · 진로 판단은 이 저장소에서 하지 마라.** "볼트에 이런 사실이 생겼다"만 전한다
- 양쪽을 동시에 보는 것은 **신이안이 명시적으로 요청할 때만** — 그 세션에 한해
  `add_repo`로 붙인다. AI가 알아서 붙이지 않는다.
  git submodule로 영구히 잇는 방법은 쓰지 않는다(볼트의 주소가 드러난다)

**사람이 오갈 때는 옵시디언 URI를 쓴다**: `[볼트의 STSY 노트](obsidian://open?vault=Vault&file=STSY)`
— `vault=` 에 보관함 이름, `file=` 에 확장자 뺀 경로. 공백·한글은 URL 인코딩.
옵시디언 앱에서만 동작하고 GitHub 웹에서는 죽은 링크다.

> ⚠ **이 파일과 [`CLAUDE.md`](CLAUDE.md)의 「볼트와의 경계」 절은 볼트의 존재를 드러낸다.**
> 저장소를 공유하거나 공개할 계획이 생기면 **먼저 이 둘을 지우고 git 이력도 재작성한다.**

---

## → 볼트로 보낼 것

### 1. STSY 저장소 링크 추가

**대상 파일**: 볼트의 `STSY.md`

> 연구 실무(장치 설계 · 신호 처리 · 문헌)는 STSY 저장소에 있다 —
> https://github.com/ianshin123/STSY

---

## ← 볼트에서 온 것

*(아직 없음)*

## 보낸 것

*(아직 없음)*
